# Enable rounded corners
backend = "glx";
corner-radius = 10;  # Adjust this value for more or less roundness
rounded-corners = true;

# Optionally, set a shadow around the windows:
shadow = true;
shadow-radius = 12;
shadow-offset-x = -12;
shadow-offset-y = -12;

